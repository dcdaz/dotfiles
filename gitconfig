[user]
	name = Daniel Córdova A.
	email = REDACTED
[includeIf "gitdir:~/work/"]
    path = ~/work/.gitconfig
[core]
	editor = vim
[merge]
	tool = vimdiff
[diff]
    tool = vimdiff
[alias]
    a = add .
    b = branch
    bl = blame
    br = branch -r
    c = commit
    cl = clone
    co = checkout
    cob = checkout -b
    d = diff
    f = fetch
    g = grep -i
    l = log
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset %C(bold green)<%aE>%Creset' --abbrev-commit --date=relative
    lbg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset %C(bold green)<%aE>%Creset' --abbrev-commit --date=relative --branches
    m = merge
    p = pull
    pu = push
    r = reset
    rh= reset --hard
    s = show
    st = status

